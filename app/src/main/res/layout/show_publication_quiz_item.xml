<?xml version="1.0" encoding="utf-8"?>
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/ShowPublication_QI_AnswerBlock"
    style="@style/MW"
    android:layout_height="30dip"
    android:background="@color/quiz_grey">

    <ProgressBar
        android:id="@+id/ShowPublication_QI_Progress"
        style="@style/CustomProgressBar"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_gravity="center_vertical"
        android:visibility="gone"/>
    <TextView
        android:id="@+id/ShowPublication_QI_AnswerText"
        style="@style/WW"
        android:layout_marginLeft="10dp"
        android:layout_gravity="center_vertical"
        android:textColor="@color/dark_grey"
        android:text="Answer Text" />
    <TextView
        android:id="@+id/ShowPublication_QI_AnswersSum"
        style="@style/WW"
        android:layout_marginRight="10dp"
        android:layout_gravity="center_vertical|right"
        android:textStyle="bold"
        android:textColor="@color/dark_grey"
        android:text="0" />

</FrameLayout>